services:
  search:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - asset-data:/srv/app/data
    environment:
      - ASSET_DB=/srv/app/data/assets.db
    restart: unless-stopped
volumes:
  asset-data:
